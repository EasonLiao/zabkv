#!/bin/bash

if [ "$#" -le 1 ]; then
  echo "usage: $0 http-port -DserverId=localhost:5000"
  echo "usage: $0 http-port -DserverId=localhost:5001 -Djoin=localhost:5001"
  echo "usage: $0 http-port -DlogDir=localhost:5000"
  exit 1
fi

mvn compile ${2} ${3} -e exec:java -Dexec.args="$1"
